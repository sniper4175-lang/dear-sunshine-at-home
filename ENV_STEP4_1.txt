# .env.local 에 추가
# 실제 값은 GitHub에 올리지 마세요.

# 토스페이먼츠 개발자센터 > API 개별 연동 키 > 테스트 키
NEXT_PUBLIC_TOSS_CLIENT_KEY=test_ck_여기에_테스트_클라이언트키
TOSS_SECRET_KEY=test_sk_여기에_테스트_시크릿키

# billingKey AES-256-GCM 암호화용 32바이트 키를 base64로 저장
# PowerShell에서 한 번 생성:
# $bytes = New-Object byte[] 32
# [Security.Cryptography.RandomNumberGenerator]::Fill($bytes)
# [Convert]::ToBase64String($bytes)
BILLING_ENCRYPTION_KEY=여기에_생성된_base64_값

# 기존 Supabase 환경변수는 그대로 유지
